# PostgreSQL Database Connection String (Optional: falls back to embedded PG database if not set)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/portfolio_db

# Secret key for JWT Authentication in Admin Portal
JWT_SECRET=super-secret-jwt-key-change-in-production-12345
